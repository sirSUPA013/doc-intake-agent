[pytest]
asyncio_mode = auto
pythonpath = src .
testpaths = tests
addopts = -q -m "not e2e and not integration"
markers =
    e2e: end-to-end browser tests (slow; run with -m e2e)
    integration: live tests that call the real model (cost money; run with -m integration)
